# MNTrapTeam 3.5.3 — MyATA Search/Buddies Capture

The authenticated `GetMemberStatsSummary` endpoint was confirmed to be
self-account scoped: the logged-in shooter succeeds, while other ATA numbers
return HTTP 401.

This diagnostic captures the API traffic generated by MyATA's intended
other-shooter workflow: Search/Buddies.

Run:

    & ".\.venv\Scripts\python.exe" -m mntrapteam.myata_search_cli

Then search for ATA 2523333 and open any available shooter details/statistics.

No passwords or authorization headers are printed or saved by this capture.
